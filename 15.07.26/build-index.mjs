// Composes index.html for 15.07.2026, reusing the house stylesheet from the previous report verbatim
// so the series stays visually identical.
import fs from 'node:fs'
import path from 'node:path'

const prev = fs.readFileSync(path.join(import.meta.dirname, '..', '14.07.26', 'index.html'), 'utf8')
const style = prev.slice(prev.indexOf('<style>'), prev.indexOf('</style>') + 8)

const shots = [
  [
    '01-dispatchers-table.png',
    'Диспетчеры · таблица',
    'изменено',
    'Колонка <b>«Телефон / email»</b> вместо «Телефон»: строка 4 (Paul Allen) показывает <b>и телефон, и email</b>, строки 8/14/15/17 — только email. Роли перекрашены: <b>Driver manager — зелёный</b>, <b>Cargo manager — синий</b> (было синий vs geekblue — неразличимо). Строка 8 в колонке «Статус» теперь «Pending», а не сырое <span class="mono">pending</span>.'
  ],
  [
    '02-dispatchers-date-columns.png',
    'Диспетчеры · три новые колонки',
    'новое',
    '<b>Registered · Updated · Last online</b> — добавлены перед колонкой «Действия», как и просили. Сортировка идёт по разобранному моменту времени, а не по строке: смещения вида <span class="mono">+05:00</span> не сортируются лексикографически против <span class="mono">Z</span>.'
  ],
  [
    '03-dispatcher-drawer.png',
    'Диспетчер · карточка просмотра',
    'новое',
    'Модалка переписана <b>под вид карточки водителя</b> (секции с иконками вместо плоского списка). <b>Фото реально загружается</b> (снимок 358×347 через авторизованный <span class="mono">/photo</span>). Секции: Контакты (телефон + email + подтверждён ли email), Личные данные, Паспорт, Активность.'
  ],
  [
    '04-dispatcher-drawer-system.png',
    'Диспетчер · системная секция',
    'новое',
    'Показаны <b>все 24 поля</b>, которые возвращает <span class="mono">/v1/admin/dispatchers</span> — проверено программно: «полей, отрисованных в модалке, но отсутствующих в ответе: <span class="mono">[]</span>» и «полей ответа, не показанных в модалке: <span class="mono">[]</span>». ID — копируемые.'
  ],
  [
    '05-dispatchers-ru.png',
    'Диспетчеры · русская локаль',
    'i18n',
    'Все новые ключи заведены во <b>все 15 локалей</b>: «Телефон / email», «Регистрация», «Обновлено», «Был онлайн», статус «На рассмотрении».'
  ],
  [
    '06-moderation-drivers-search.png',
    'Модерация · водители — поиск',
    'новое',
    'Один инпут ищет <b>по имени, телефону и email сразу</b>. Раньше на этой вкладке поиска не было вовсе.'
  ],
  [
    '07-moderation-managers-search.png',
    'Модерация · менеджеры — поиск',
    'новое',
    'То же поле. Важно именно здесь: менеджер может быть <b>зарегистрирован только по email</b> (без телефона) — поиск только по телефону его никогда бы не нашёл.'
  ],
  [
    '08-moderation-passport-search.png',
    'Модерация · паспорт — поиск',
    'новое',
    'Строки очереди KYC <b>не содержат email</b>, поэтому здесь поиск честно ограничен именем и телефоном.'
  ],
  [
    '09-moderation-companies-search.png',
    'Модерация · компании — поиск',
    'новое',
    'Для компании ключи поиска — <b>название, телефон и ИНН</b> (плейсхолдер отличается: «Search by name, phone or INN…»).'
  ],
  [
    '10-moderation-log.png',
    'Журнал модерации · поиск',
    'новое',
    'Видимое поле поиска рядом с фильтрами. Строка журнала <b>не содержит телефона</b> — искать можно по <b>названию объекта</b> (<span class="mono">target_label</span>), <b>модератору</b> (<span class="mono">admin_login</span>) и id.'
  ],
  [
    '11-moderation-log-search-active.png',
    'Журнал модерации · поиск применён',
    'проверено',
    'Живая проверка: поиск по имени объекта сузил список с <b>50 до 4 строк</b>; поиск по модератору вернул 50. Опции колоночных фильтров при этом <b>не схлопываются</b> — это был баг, найденный на самопроверке и исправленный.'
  ],
  [
    '12-trips-table.png',
    'Рейсы · колонка «Обновлено» + цена',
    'изменено',
    'Добавлена колонка <b>UpdatedAt</b> перед «Действиями» (рядом с «Создан»). Цена теперь <b>через общий <span class="mono">formatPrice</span></b>: <span class="mono">$50,000,000,000,000</span> вместо нечитаемой простыни нулей.'
  ],
  [
    '13-trip-drawer.png',
    'Рейс · карточка просмотра',
    'новое',
    'Плоский список <span class="mono">Descriptions</span> заменён на <b>тот же вид, что у водителя и диспетчера</b>. Секции: Рейс, Хронология (9 отметок времени), Транспорт, Назначение, Системная информация.'
  ],
  [
    '14-trip-drawer-vehicle-system.png',
    'Рейс · транспорт и система',
    'новое',
    '<span class="mono">VehicleSnapshot</span> приходит как <b>base64-JSON</b> — раскодирован и показан по полям. Типы платформ идут через справочник <span class="mono">truckTypeLabel</span>: <b>«Truck» / «Flatbed»</b>, а не сырые enum-константы.'
  ],
  [
    '15-trips-ru.png',
    'Рейсы · русская локаль',
    'i18n',
    'Заголовки «Создан» / «Обновлено» и все ключи карточки заведены во все 15 локалей. Разделитель разрядов берётся из активного языка: <span class="mono">$12 222</span> в ru против <span class="mono">$12,222</span> в en.'
  ]
]

const shotHtml = shots
  .map(
    ([file, title, tag, cap]) => `    <div class="ss-item">
      <img src="img/${file}" alt="${title}" />
      <div class="ss-cap"><b>${title}</b><span class="tag-change">${tag}</span> — ${cap}</div>
    </div>`
  )
  .join('\n')

const html = `<!DOCTYPE html>
<html lang="ru">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>Отчёт о работе · 15.07.2026 · Sarbon Frontend</title>
${style}
</head>
<body>

<header class="top">
  <div class="wrap">
    <div class="kicker">Отчёт о проделанной работе · для PM, QA и стейкхолдеров</div>
    <h1>Итоги за&nbsp;15 июля 2026 — Sarbon Frontend</h1>
    <div class="sub">День <b>админки</b>. Страница <b>«Диспетчеры»</b> приведена к виду «Водителей»: колонка <b>«Телефон / email»</b> (раньше email-менеджеры были не видны), три новые колонки — <b>Регистрация / Обновлено / Был онлайн</b>, и <b>новая карточка просмотра</b> с реальным фото и <b>всеми 24 полями</b>, которые отдаёт API. Роли перекрашены в <b>зелёный / синий</b>. Добавлен <b>единый поиск по имени и телефону</b> на все очереди модерации и в журнал модерации. На <b>«Рейсах»</b> — колонка «Обновлено» и такая же полноценная карточка (с раскодированным снимком транспорта). Отдельно — <b>длинные списки больше не ждут полной загрузки</b>: первые строки появляются сразу, остальное дозагружается фоном.</div>
    <div class="meta-row">
      <span><b>Проект:</b> sarbon-frontend (React 19 · Vite · Ant Design 6)</span>
      <span><b>Ветка:</b> staging</span>
      <span><b>Тесты:</b> <span class="mono">1311 / 1311</span> (144 файла)</span>
      <span><b>Объём:</b> <span class="tag">диспетчеры</span> <span class="tag">поиск в модерации</span> <span class="tag">рейсы</span> <span class="tag">прогрессивная загрузка</span> <span class="tag">самопроверка кода</span></span>
    </div>
  </div>
</header>

<div class="wrap">

<section id="summary">
  <h2><span class="n">i</span> Краткое резюме</h2>
  <p class="lead">Пять задач по странице «Диспетчеры», поиск в модерации, доработка «Рейсов» и ускорение длинных списков. Всё проверено вживую в браузере на staging-API, а не только тестами.</p>
  <div class="stats">
    <div class="stat blue"><div class="v">5</div><div class="l">задач по диспетчерам</div></div>
    <div class="stat good"><div class="v">15</div><div class="l">скриншотов (живые)</div></div>
    <div class="stat good"><div class="v">1311</div><div class="l">тестов зелёные</div></div>
    <div class="stat blue"><div class="v">+9</div><div class="l">новых тестов</div></div>
    <div class="stat warn"><div class="v">6</div><div class="l">багов найдено самопроверкой</div></div>
    <div class="stat good"><div class="v">15</div><div class="l">локалей обновлено</div></div>
  </div>

  <div class="card">
    <h3>Что увидит пользователь</h3>
    <table>
      <tr><th style="width:210px">Место</th><th>Что изменилось</th></tr>
      <tr><td><b>Админка · Диспетчеры</b></td><td>Колонка «Телефон / email» показывает оба контакта (или только email — таких менеджеров раньше не было видно). Три новые колонки: Регистрация, Обновлено, Был онлайн. Кнопка «Просмотр» открывает карточку как у водителя — с фото и всеми данными. Роли: <b>Driver manager — зелёный</b>, <b>Cargo manager — синий</b>.</td></tr>
      <tr><td><b>Админка · Модерация</b></td><td>На вкладках «Водители», «Менеджеры», «Паспорт», «Компании» появилось поле поиска — <b>одно поле ищет и по имени, и по телефону</b> (и по email / ИНН, где они есть).</td></tr>
      <tr><td><b>Админка · Журнал модерации</b></td><td>Появился видимый поиск: по названию объекта и по модератору.</td></tr>
      <tr><td><b>Админка · Рейсы</b></td><td>Колонка «Обновлено». Карточка рейса переписана: хронология из 9 отметок, раскодированные данные транспорта, читаемая цена.</td></tr>
      <tr><td><b>Длинные списки</b></td><td>Журнал модерации и другие большие таблицы <b>показывают первые строки сразу</b> (≈1,9 с) и молча дозагружают остальное — раньше экран ждал загрузки всех страниц целиком.</td></tr>
    </table>
  </div>
</section>

<section id="changes">
  <h2><span class="n">1</span> Все изменения дня <small>диспетчеры · поиск в модерации · рейсы · загрузка списков</small></h2>

  <div class="card">
    <h3>1. Диспетчеры — пять задач из запроса</h3>
    <table>
      <tr><th style="width:44px">№</th><th style="width:230px">Задача</th><th>Как сделано</th></tr>
      <tr><td>1</td><td>Колонка «Телефон» → «Телефон / email»</td><td>Показывает телефон, email или оба — как на странице водителей. Поиск теперь тоже покрывает email (раньше email-менеджера нельзя было найти). На staging: 2 строки из 36 с обоими контактами, 4 — только email.</td></tr>
      <tr><td>2</td><td>Три колонки дат в конце</td><td><span class="mono">created_at</span>, <span class="mono">updated_at</span>, <span class="mono">last_online_at</span> — перед «Действиями».</td></tr>
      <tr><td>3</td><td>Вид карточки как у водителя</td><td>Новый компонент <span class="mono">AdminDispatcherDetailDrawer</span> с той же вёрсткой секций и темизацией.</td></tr>
      <tr><td>4</td><td>Все данные из запроса + фото</td><td>Отрисованы все 24 поля ответа. Фото — через новый хук <span class="mono">useAdminDispatcherPhoto</span>.</td></tr>
      <tr><td>5</td><td>Цвета ролей</td><td>Driver manager — зелёный, Cargo manager — синий.</td></tr>
    </table>
    <p class="lead" style="margin-top:12px"><b>Про фото у водителя.</b> В запросе было «в карточке водителя фото не подключено — подключить». Проверка показала, что <b>оно уже подключено и работает</b>: просто у 5 водителей из 50 на staging есть фото (<span class="mono">has_photo=true</span>), у остальных его нет — поэтому и казалось, что не работает. Ничего чинить не потребовалось; для диспетчеров сделан такой же хук.</p>
  </div>

  <div class="card">
    <h3>2. Поиск в модерации и журнале</h3>
    <p class="lead">Одно поле на очередь, ищет сразу по нескольким полям. Вынесены общий компонент <span class="mono">AdminSearchInput</span> и чистая функция <span class="mono">filterAdminRowsBySearch</span> (покрыта 9 тестами) — вместо пятой копии одного и того же кода.</p>
    <table>
      <tr><th style="width:220px">Экран</th><th>По каким полям ищет</th></tr>
      <tr><td>Модерация · водители</td><td>имя + телефон + email</td></tr>
      <tr><td>Модерация · менеджеры</td><td>имя + телефон + email</td></tr>
      <tr><td>Модерация · паспорт</td><td>имя + телефон <i>(email в этих строках не приходит)</i></td></tr>
      <tr><td>Модерация · компании</td><td>название + телефон + ИНН</td></tr>
      <tr><td>Журнал модерации</td><td>название объекта + модератор + id <i>(телефона в строке журнала нет)</i></td></tr>
    </table>
  </div>

  <div class="card">
    <h3>3. Рейсы</h3>
    <table>
      <tr><th style="width:220px">Что</th><th>Детали</th></tr>
      <tr><td>Колонка «Обновлено»</td><td><span class="mono">UpdatedAt</span> перед «Действиями» (у рейсов поля приходят в PascalCase — это особенность именно этого эндпоинта).</td></tr>
      <tr><td>Полная карточка</td><td>Секции: Рейс · Хронология · Транспорт · Назначение · Системная информация.</td></tr>
      <tr><td>Данные транспорта</td><td><span class="mono">VehicleSnapshot</span> — base64-JSON; раскодирован, типы платформ через справочник («Truck», «Flatbed»).</td></tr>
      <tr><td>Цена</td><td>Через общий <span class="mono">formatPrice</span> — разделитель разрядов по активному языку.</td></tr>
    </table>
  </div>

  <div class="card">
    <h3>4. Длинные списки грузятся постранично</h3>
    <p class="lead">Раньше таблица ждала, пока прогрузятся <b>все</b> страницы, и до этого не показывала ничего — отсюда ощущение, что журнал модерации «висит». Теперь первая страница <b>публикуется сразу</b>, остальные дописываются по мере прихода, спиннер таблицу не блокирует. Обновление по кнопке по-прежнему применяется одним разом — иначе таблица на глазах схлопывалась бы до 100 строк и разрасталась заново.</p>
    <table>
      <tr><th style="width:220px">Измерено вживую</th><th>Результат</th></tr>
      <tr><td>Первые строки журнала</td><td><b>≈1,9 с</b> (при искусственном замедлении сети), блокирующего спиннера нет</td></tr>
      <tr><td>Догрузка</td><td>список продолжает расти после первой отрисовки (6 → 8 страниц) и молча становится полным</td></tr>
      <tr><td>Защита от «половины списка»</td><td>прерванная загрузка помечается как неполная и возобновляется — неполный список не может быть показан как полный</td></tr>
    </table>
  </div>
</section>

<section id="quality">
  <h2><span class="n">2</span> Качество и проверки</h2>

  <div class="card">
    <h3>Самопроверка кода — 6 замечаний к собственному коду, все исправлены</h3>
    <p class="lead">После реализации прогнан код-ревью по своему же диффу. Найденное чинилось до сдачи:</p>
    <table>
      <tr><th style="width:44px">№</th><th style="width:240px">Проблема</th><th>Исправление</th></tr>
      <tr><td>1–2</td><td>Цена рейса форматировалась с <b>жёстко зашитой русской локалью</b> и дублировала уже существующий <span class="mono">formatPrice</span></td><td>Переведено на общий <span class="mono">formatPrice</span>. Проверено: en — <span class="mono">$12,222</span>, ru — <span class="mono">$12 222</span></td></tr>
      <tr><td>3</td><td>Поиск <b>сужал опции колоночных фильтров</b> — можно было «застрять» с применённым фильтром, галочку которого уже не видно</td><td>Фильтры снова строятся по всей очереди, а не по найденному подмножеству</td></tr>
      <tr><td>4</td><td>Возобновлённая загрузка <b>не показывала прогресс</b> и молча выдавала неполный список за полный</td><td>Стриминг включается, пока нет полного набора, а не только на первой загрузке</td></tr>
      <tr><td>5</td><td>Возобновление игнорировало флаг «запрос выключен» — могло фоном тянуть весь список без нужды</td><td>Добавлена проверка</td></tr>
      <tr><td>6</td><td>Открытая карточка теряла подставленные данные, если поиск исключал её строку</td><td>Строка ищется по всей очереди</td></tr>
    </table>
  </div>

  <div class="card">
    <h3>Автоматические проверки</h3>
    <table>
      <tr><th style="width:260px">Проверка</th><th>Результат</th></tr>
      <tr><td>Типы (<span class="mono">tsc -b --force</span>)</td><td><span class="badge b-ok">чисто</span></td></tr>
      <tr><td>Линтер (<span class="mono">eslint . --max-warnings 0</span>)</td><td><span class="badge b-ok">чисто</span></td></tr>
      <tr><td>Тесты (<span class="mono">vitest run</span>)</td><td><span class="badge b-ok">1311 / 1311</span> в 144 файлах (+9 новых на поиск)</td></tr>
      <tr><td>Локали</td><td><span class="badge b-ok">15 / 15</span> — все новые ключи заведены во все языки</td></tr>
    </table>
    <p class="lead" style="margin-top:12px"><b>Честное замечание.</b> В одном из прогонов два теста рендера упали по таймауту. Причина — не код: параллельно работали dev-сервер и браузер Playwright и забирали процессор. По отдельности тесты проходят за 0,5 с при лимите 5 с, на свободной машине весь набор зелёный. Это хрупкость самих тестов, а не регресс.</p>
  </div>

  <div class="card">
    <h3>Живые проверки (Playwright, staging-API)</h3>
    <table>
      <tr><th style="width:280px">Что проверено в браузере</th><th>Результат</th></tr>
      <tr><td>Колонки диспетчеров</td><td>№, Имя, Телефон / email, Роль, Статус, Модерация, Рейтинг, Грузы/офферы/рейсы, Регистрация, Обновлено, Был онлайн, Действия</td></tr>
      <tr><td>Стек «телефон + email»</td><td>2 строки из 36 с обоими, 4 — только email</td></tr>
      <tr><td>Цвета ролей</td><td>Driver manager — <span class="mono">ant-tag-green</span>, Cargo manager — <span class="mono">ant-tag-blue</span></td></tr>
      <tr><td>Фото в карточке</td><td>реальный снимок загружен (358×347)</td></tr>
      <tr><td>Полнота карточки</td><td>полей в модалке без ответа: <span class="mono">[]</span> · полей ответа без модалки: <span class="mono">[]</span></td></tr>
      <tr><td>Статус «Pending»</td><td>«Pending» / «На рассмотрении» вместо сырого <span class="mono">pending</span></td></tr>
      <tr><td>Поиск в журнале</td><td>по имени — 4 из 50 строк, по модератору — 50</td></tr>
      <tr><td>Поиск на всех вкладках модерации</td><td>работает; опции колоночных фильтров не схлопываются</td></tr>
      <tr><td>Ошибки в консоли</td><td><span class="badge b-ok">нет</span></td></tr>
    </table>
  </div>
</section>

<section id="shots">
  <h2><span class="n">3</span> Скриншоты <small>живой Playwright · staging-API · 15 снимков</small></h2>
  <p class="lead">Все снимки сделаны в реальном браузере на реальных данных staging — не макеты.</p>
  <div class="ss-grid">
${shotHtml}
  </div>
</section>

</div>

<footer>
  <div class="wrap">
    Отчёт сформирован 15.07.2026 · sarbon-frontend · ветка staging · скриншоты: живой Playwright на staging-API (админка) · файлы: report-15.07.26.xlsx, img/*.png
  </div>
</footer>

</body>
</html>
`

fs.writeFileSync(path.join(import.meta.dirname, 'index.html'), html, 'utf8')
console.log('index.html written —', html.length, 'bytes,', shots.length, 'screenshots')
